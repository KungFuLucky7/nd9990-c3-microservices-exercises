language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "Nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "No tests"

script:
  - docker --version # Print the version for logging
  - docker build -t simple-node ./nd9990-c3-microservices-exercises/lesson-2-containers/exercises/simple-node
  - docker tag simple-node kungfulucky7/simple-node:latest

# Tasks to perform after the process is successful.
# Formatting the Docker username and password as below enables you
# to programmatically log in without having the password exposed in logs.
after_success:
#  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
#  - docker push kungfulucky7/simple-node
  - docker images
